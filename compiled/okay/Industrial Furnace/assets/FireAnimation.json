{
	// the offset scale: 64 is one tile
	// should the mod load a custom sprite
	"UseCustomSprite": false,
	// time between spawn attempts in milliseconds
	"SpawnFrequency": 500,		
	// chance to spawn each try
	"SpawnChance": 0.2,
	// spawnpoint offset from the building's point
	"SpawnXOffset": 64,
	"SpawnYOffset": 18,
	// the limit of random offset
	"SpawnXRandomOffset": 16,
	"SpawnYRandomOffset": 4,
	// the dimensions of one frame of the animation
	"SpriteSizeX": 64,
	"SpriteSizeY": 64,
	// time between frames in milliseconds
	"AnimationSpeed": 100,
	// how many frames the animations consists of
	"AnimationLength": 4,
	// chance for the sound effect to happen
	"SoundEffectChance": 0.05,
	// the light source's offset from the building's point
	"LightSourceXOffset": 96,
	"LightSourceYOffset": 96,
	// scale for the light source's size
	"LightSourceScaleMultiplier": 1.5
}
/*************************************************
**
** You're viewing a file in the SMAPI mod dump, which contains a copy of every open-source SMAPI mod
** for queries and analysis.
**
** This is *not* the original file, and not necessarily the latest version.
** Source repository: https://github.com/sophiesalacia/StardewMods
**
*************************************************/

using StardewValley;

namespace Fireworks;

internal class ConsoleCommandManager
{
    internal static void InitializeConsoleCommands()
    {
        /*Globals.CCHelper.Add("sophie.mj.testevent", "Test mod jam event", (command, args) =>
            {
                const string eventString = "none/0 0/farmer 10 10 0/skippable/pause 1000/cutscene ModJam/pause 1000/globalFadeToClear/end";

                GameLocation currentLoc = Game1.currentLocation;
                currentLoc.startEvent(new Event(eventString));
            }
        );*/
    }
}

namespace TehPers.Core.Enums {
    public enum Alignment {
        LEFT,
        CENTER,
        RIGHT,
        TOP = Alignment.LEFT,
        MIDDLE = Alignment.CENTER,
        BOTTOM = Alignment.RIGHT,
    }
}
